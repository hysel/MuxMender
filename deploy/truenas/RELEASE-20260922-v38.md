# MuxMender 20260922-v38

This release fixes HDR packaging timestamps using decoded pictures rather than
container packet timestamps. It preserves original timing without relaxing the
validation checks. CLI and app use the same implementation.

Development reporting now distinguishes incomplete evaluations from legitimate
keep-original decisions. STOP-path regression coverage prevents full encoding
or publication after interruption.

Evidence: 699 Linux tests passed with five environment-dependent skips. The
older NVIDIA generated HDR10 regression passed with 76.69% smaller output.
Full-length experimental DV/HDR10+ qualification passed with 69.80% reduction;
experimental publication remains disabled and production guards remain enabled.
The separate validation benchmark reduced validation time by 30.6%, not total
conversion time. Retained test copies do not count as reclaimed library space.

Build image `muxmender-app:20260922-v38` from the staged context. Keep existing
mounts and app settings. Deploy before resuming pending work. No research output
is automatically published and no source media is modified by deployment.
