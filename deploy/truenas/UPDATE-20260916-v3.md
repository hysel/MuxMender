# Dashboard identification update and next sample comparison

Build context uploaded to `/mnt/FR4G/Apps/muxmender/app-build-20260916-v3`.
No image is published. The current container is not changed by this upload.

After confirming no job is running, run in the TrueNAS administrator shell:

```bash
sudo docker build \
  -f /mnt/FR4G/Apps/muxmender/app-build-20260916-v3/deploy/truenas/Dockerfile.app \
  -t muxmender-app:20260916-v3 \
  /mnt/FR4G/Apps/muxmender/app-build-20260916-v3
```

Edit the existing guided Custom App (do not recreate it):

- Image tag: `20260916-v3`; keep repository `muxmender-app` and pull policy Never.
- Keep Custom User UID/GID 3005/3005, the selected GPU, password, and network settings.
- Keep `/media` read-only and `/output` writable. Do not change media ACLs.
- Set `MUXMENDER_SOURCE` to `TV/Series A/Season 1/Series A.S01E16.Episode A.1080p.BluRay.EAC3.AVC-PiR8.mkv`.
- Set `MUXMENDER_JOB_ID` to `Episode A-codec-trial-01`.
- Keep `MUXMENDER_PLAYBACK_CODECS=hevc,av1` and `MUXMENDER_FULL_COPY=false`.
- Save once. Refresh the dashboard browser page to load the new interface.

This starts three-scene HEVC/AV1 comparisons, not a full conversion. Quality
requirements remain mean VMAF >=95, fifth percentile >=90, and at least 10%
sample savings. Resolution, frame timing, copied tracks and source integrity
remain checked. No source replacement/deletion is authorized or performed.

Changes:
- Codec runs show filename, comparison mode and unique timestamp/run suffix.
- Source filenames are available in the API and details/search.
- Skipped jobs have their own count/filter, separate from completed and checks passed.
- Rejection messages distinguish quality failures from insufficient savings.
- Default focus is the active run or newest run, not an older capability report.
- Selecting history while another job runs produces an explicit live-status notice.
- The app worker summary includes externally launched tracked jobs.
- The app sidebar identifies TrueNAS/Linux rather than Windows.
- Execution success and optimization outcome remain separate API fields.

Validated locally: 277 Python tests, dashboard DOM-model tests, read-only source
eligibility. New image build and live UI verification require deployment; no claim
of successful encoding or expected savings is made before sample results.
