# Research and development handoff

## Safety and deployment

Production is on v37. This checkpoint is source code, not a new Docker release.
Keep production Dolby Vision restrictions enabled until the shared integration
has passed end-to-end qualification. Experimental output is not authorized for
replacement. Never change source media during research or resume production
without permission.

Never run Linux process probes on the Windows development host. A monitor unit
test previously executed `os.kill(pid, 0)` locally, coinciding with repeated
Codex console interruption exits. The reader now uses read-only Linux `/proc`
checks, rejects non-Linux execution, and its tests are Linux-only. Do not restore
signal-based liveness checks. Run qualification tests on the Linux test server.

## Confirmed results

- Older NVIDIA/Pascal representative: full-length SDR HEVC output passed shared
  validation with 42.92% potential reduction. Original retained.
- Latest generated SDR workflow check: passed, 65.83% smaller, 15.9 seconds,
  using the 25% savings threshold. Synthetic evidence is not certification of
  all older cards or all real media.
- Real HDR candidates on that older GPU were legitimate quality/savings keeps;
  successful real HDR end-to-end qualification remains outstanding.
- TrueNAS combined Dolby Vision/HDR10+ adapter trials at CQ30 and CQ28 both
  completed with keep-original decisions. CQ30 failed a measured scene's quality
  floor; inspect CQ28's retained report before choosing another setting. These
  trials did not establish full shared-adapter integration success.
- Earlier specialized full-file preservation evidence exists, but it does not
  replace qualification of the new automatic adapter.

## Dashboard and remaining work

Development dashboard: loopback port 8765. Production dashboard: server port
8767. Do not confuse the two. Remote research is mirrored by
`tools/monitor_remote_research.py`; every long development job must be registered
and its dashboard API entry verified. Both most recent media tests have finished;
there is no active conversion from those launches.

The observer missed nested automatic-workflow output reports. Recursive outcome
discovery and a regression test are included here. Refresh the existing observer
to correct its saved summary; do not rerun media just to repair dashboard counts.

Next work:

1. Refresh the observer and verify both latest outcomes on the dashboard.
2. Inspect the CQ28 rejection evidence; qualify the adapter's full-output path
   without lowering agreed quality floors or altering originals.
3. On the older NVIDIA representative, qualify HDR, benchmark validation cost,
   and verify disposable artifact cleanup. Use generic capability decisions,
   not model-name special cases. AV1 unavailability should allow HEVC selection.
4. Keep metadata preservation, stream timing, original resolution, minimum
   savings and publication checks shared between CLI and app.
5. Build a new app image only when requested. Future packaging requires the
   checksum-pinned dovi_tool vendor archive in addition to hdr10plus_tool.

Private SSH identities, exact source paths, runtime reports and monitor configs
remain outside this public repository in the local tools and test-output folders.
